<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.plgrim.ncp.commons.god">

<select id="selectListShopId" parameterType="java.util.Map" resultType="string"> 
 SELECT img_url
  FROM GOD_IMG
 WHERE GOD_NO      = #{godNo, jdbcType=VARCHAR}
   AND IMG_TURN    = #{imgTurn, jdbcType=VARCHAR}
   AND IMG_TP_CD   = 'THNAIL' /* 상품 이미지 */
   AND IMG_SIZE_CD = #{size, jdbcType=VARCHAR}  /*가로X세로 */
</select>

</mapper>
	